-r requirements.txt

# --- Testing ---
pytest==9.0.3
pytest-asyncio==0.24.0           # async test support
pytest-cov==6.0.0                # coverage reports
httpx==0.27.2                    # async HTTP client for TestClient

# --- Code Quality ---
black==24.10.0                   # code formatter
ruff==0.8.4                      # fast linter (replaces flake8, isort)
mypy==1.13.0                     # static type checker

# --- DB Testing ---
aiosqlite==0.20.0                # in-memory SQLite for fast DB tests
