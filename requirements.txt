# ============================================================
# AiDerm Cliniq — Production Dependencies
# ============================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1        # ASGI server with websocket support

# --- Database ---
sqlalchemy[asyncio]==2.0.36      # ORM with async support
asyncpg==0.30.0                  # Async PostgreSQL driver
psycopg2-binary==2.9.10          # Sync driver (Alembic migrations only)
alembic==1.14.0                  # Database migrations

# --- Auth ---
python-jose[cryptography]==3.3.0 # JWT encoding/decoding
passlib[bcrypt]==1.7.4           # Password hashing
bcrypt==4.0.1                    # Pinned: bcrypt 4.x+ breaks passlib 1.7.4 (ValueError >72 bytes)
httpx==0.28.1                    # HTTP client (Google OAuth token exchange)

# --- Validation & Settings ---
pydantic==2.10.3
pydantic[email]==2.10.3          # EmailStr validation support
pydantic-settings==2.6.1         # BaseSettings with .env support

# --- Task Queue ---
celery[redis]==5.4.0             # Async task queue
redis==5.2.1                     # Redis client (Celery broker + cache)

# --- Rate Limiting ---
slowapi==0.1.9                   # FastAPI rate limiter

# --- File Storage ---
google-cloud-storage==2.19.0     # GCS SDK

# --- AI Providers ---
google-genai>=1.0.0              # Gemini SDK (new SDK with thinking_config support)
openai==1.59.3                   # OpenAI SDK

# --- PDF Generation ---
xhtml2pdf==0.2.17                # HTML → PDF renderer (pure Python, Windows-compatible)

# --- QR Codes ---
qrcode[pil]==8.0                 # QR code image generator

# --- Email ---
jinja2==3.1.4                    # HTML email templates

# --- Push Notifications ---
firebase-admin==6.6.0            # Firebase Admin SDK (FCM push notifications)

# --- Logging ---
structlog==24.4.0                # Structured JSON logging

# --- Utilities ---
python-multipart==0.0.20         # Multipart form data (file uploads)
pillow==12.2.0                   # Image processing (HEIC conversion, thumbnails)
